# 文苑 · 待办

状态快照：2026-09-11 首次上线时。做完一项就删一项，不要留「已完成」的条目在这里。

## 等你填的

- [ ] **散文正文还是原博客的拷贝**（16 篇），等你后期填充或替换。
      源在 `technical/source/_posts/`，见 `README.md` 的「加一篇散文」。

## 功能上还没做的

- [ ] **档案盒面上的标签是固定的**。`src/scene.ts` 的 `drawLabel()` 画的是
      `RHINE LAB, LLC. / NO.001` 这类装饰文字，不随选中文章变化。想让盒面显示
      真实标题，要给每篇各生成一张贴图（现在全场共用一张），是一次真改动。
      要不要做，看上线后的观感再定。
- [ ] **单列下的开机动画没验证过**。开发时用 `?scene=` 之类的入口绕过了开场，
      所以「白底开场 → 折叠成阵列」这段在只有一列（可见 2–3 列）时到底顺不顺，
      是在真机上没看过的事实。没事就找个桌面浏览器完整看一遍开场。
- [ ] **正文页没进 Service Worker 预缓存**。`sw.js` 走显式白名单，16 个
      `/prose/w-NNN/` 不在名单里，所以离线点「阅读全文」会穿透到网络。
      不预缓存是刻意的：安装时任何一个文件失败会连带删掉整个版本。
      要加的话在 `scripts/build-pwa.mjs` 的白名单里补一条正则。
- [ ] **`W-NNN` 编号按日期升序分配**，往中间插一篇早期文章会让它之后所有篇改号，
      已分享的 `/prose/w-007/` 会指到别的文章。要么接受，要么改成只往后追加。
      两种都行，现在选的是前者。

## 上游带过来、暂时没用的

- [ ] `src/wallpaper*.ts`、`src/audio.ts` 等是上游的壁纸引擎与音效支持。
      文苑不用，但删掉会牵动 `main.ts`，留着也不进包（构建会 tree-shake 掉未引用部分）。
      哪天想瘦身再说。
