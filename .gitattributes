# 换行符统一策略
#
# 背景：这个库既是 Hexo 博客源码，也是 Obsidian 的 vault，会在 macOS 和 Windows
# 两台机器上同时编辑。Windows 端的编辑器默认写 CRLF，而仓库里现存文本全部是 LF
# （`git ls-files --eol` 实测：index 0 个 crlf）。不统一的话，在 Windows 上只改一个
# 字再保存，整篇文件都会显示成「被替换」，diff 没法读，冲突也只能整篇二选一；
# 文苑的 prose-source.mjs 还不得不专门写一段把 CRLF 归一化。
#
# text=auto：由 git 按内容判断是不是文本；eol=lf：判定为文本的，入库和检出都用 LF。
* text=auto eol=lf

# 例外：Windows 批处理保留 CRLF，避免 cmd.exe 解析括号块和标签时出问题。
*.cmd text eol=crlf
*.bat text eol=crlf

# 明确标注二进制：避免 text=auto 误判后做换行改写，损坏文件内容。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.icns binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
*.eot binary
*.mp3 binary
*.wav binary
*.ogg binary
*.m4a binary
*.flac binary
*.mp4 binary
*.webm binary
*.glb binary
*.gltf binary
*.zip binary
*.7z binary
*.dmg binary
